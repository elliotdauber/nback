version: 1.0
frontend:
  phases:
    build:
      commands:
        - echo "Building app"
        - cp index.html dist/
        - cp style.css dist/
        - cp script.js dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  framework: html

hosting:
  S3AndCloudFront:
    indexDocument: index.html
    errorDocument: index.html